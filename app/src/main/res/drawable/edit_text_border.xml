<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"

    >
    <solid android:color="@android:color/transparent" /> <!-- Optional: set a background color, or keep transparent -->
    <stroke
        android:width="1dp"
        android:color="#c3c7c9" /> <!-- Customize border width and color -->
    <corners android:radius="10dp" />
    <!-- Optional: Add rounded corners -->
</shape>
